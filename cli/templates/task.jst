"use strict";
const Nodal = require('nodal');
/**
 * Class: {{= data.name }}
 *
 * @author {{=data.author }}
 * @since {{=data.since }}  
 */
class {{= data.name }}Task {
    /**
     *
     */
    exec(args, callback) {
        console.log('{{= data.name}} task executed');
        callback();
    }

}
module.exports = {{= data.name }}Task;
